# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

env: dev

replicaCount: 1

ingress:
  hostname: dtpdev.cerebrum.ae
  path: /

appenv:
  baseapiurl: "http://dtpdev.cerebrum.ae/api/"
  debugmode: true
